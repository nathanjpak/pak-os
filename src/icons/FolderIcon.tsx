import Folder from "../../public/icons/folder.svg";

const FolderIcon = () => {
  return (
    <img className="self-center" src={Folder} height={"40px"} width={"40px"} />
  );
};

export default FolderIcon;
